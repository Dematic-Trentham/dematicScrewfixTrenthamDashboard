name: "coverage"
on:
  pull_request_target:
    branches:
      - master
      - main
      - testing
jobs:
  coverage:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - uses: ArtiomTr/jest-coverage-report-action@v2
