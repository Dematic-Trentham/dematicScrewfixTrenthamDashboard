// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model errors {
  ID        Int       @id @default(autoincrement())
  error     String    @unique @default("0")
  lastIssue DateTime? @default(now())
}

model parameters {
  ID           Int       @id @default(autoincrement())
  parameter    String    @unique @default("0") @db.VarChar(75)
  value        String    @db.Text
  lastModified DateTime? @default(now()) @db.Timestamp(0)
  hidden       Bytes?    @default(dbgenerated("0x31")) @db.Binary(1)

  @@index([parameter], map: "key")
}

model User {
  id          String   @id @default(uuid())
  email       String   @unique
  name        String
  department  String   @default("")
  profilePic  String   @default("")
  password    String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  permissions String   @default("")
}

model UserPermissions {
  id          String  @id @default(uuid())
  name        String  @unique
  description String?
}
